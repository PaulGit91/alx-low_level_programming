#include "main.h"
/**
 * is_prime_number - determines if a number is prime number
 * @n: input number
 * Return: 1 if input is prime number
 */
int is_prime_number(int n)
{
	if (n <= 1)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
