#include <stdio.h>
/**
 * main - ENTRY
 * Return: Always 0
 */
int main(void)
{
int x;
for (x = 'a'; x <= 'z'; x++)
{
if (x != 'e' && x != 'q')
{
putchar(x);
}
else
{
}
}
 putchar('\n');
return (0);
}
